
/**
 * Crea un comando de chat
 * @param {import("../../types").ChatInputCommand} data El comando
 * @returns {import("../../types").ChatInputCommand}
 */
function ChatCommand(data) {
  return data;
}
module.exports = { ChatCommand };
